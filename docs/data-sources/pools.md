---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "xenorchestra_pools Data Source - terraform-provider-xenorchestra"
subcategory: ""
description: |-
  Use this data source to filter Xenorchestra pools by certain criteria (tags) for use in other resources.
---

# xenorchestra_pools (Data Source)

Use this data source to filter Xenorchestra pools by certain criteria (tags) for use in other resources.

## Example Usage

```terraform
# Get all pools
data "xenorchestra_pools" "all" {
}

# Get pools with specific tags
data "xenorchestra_pools" "tagged" {
  tags = ["production", "web"]
}

# Get all pools sorted by name
data "xenorchestra_pools" "sorted" {
  sort_by = "name_label"
  sort_order = "asc"
}

# Use the pools data source to get pool IDs
data "xenorchestra_pools" "example" {
  tags = ["terraform-managed"]
}

data "xenorchestra_sr" "local_storage" {
  name_label = "Local storage"
  pool_id = data.xenorchestra_pools.example.pools[0].id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `sort_by` (String) The pool field to sort the results by (id and name_label are supported).
- `sort_order` (String) Valid options are `asc` or `desc` and sort order is applied to `sort_by` argument.
- `tags` (Set of String) The tags (labels) applied to the given entity. Not used for filtering if empty.

### Read-Only

- `id` (String) The ID of this resource.
- `pools` (List of Object) The resulting pools after applying the argument filtering. (see [below for nested schema](#nestedatt--pools))

<a id="nestedatt--pools"></a>
### Nested Schema for `pools`

Read-Only:

- `cpus` (Map of String)
- `default_sr` (String)
- `description` (String)
- `id` (String)
- `master` (String)
- `name_label` (String)
